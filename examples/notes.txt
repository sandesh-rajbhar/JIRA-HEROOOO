- Investigated JWT refresh failures after idle timeout
- Added null checks and request validation around session payload handling
- Fixed dashboard skeleton loader flicker on slow responses
- Synced with QA on bulk upload regression and next test cycle
